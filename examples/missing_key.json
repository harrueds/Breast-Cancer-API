{
    "_comment": "INVALID test payload for API input validation",
    "_purpose": "This file intentionally omits the required 'features' key in order to test whether the API correctly detects missing mandatory fields.",
    "_error_type": "Missing required key",
    "_specific_error": {
        "description": "The JSON body does not contain the mandatory 'features' field.",
        "missing_field": "features",
        "expected_format": "{ \"features\": [30 numeric values] }"
    },
    "_usage": "curl -X POST http://127.0.0.1:5000/predict -H 'Content-Type: application/json' -d @missing_key.json",
    "_expected_response": {
        "http_code": 400,
        "error_type": "Bad Request / Validation Error",
        "message": "Missing required field: 'features'."
    },
    "_model_impact": "Without the 'features' array, the API cannot construct the input vector required for inference, and prediction must be rejected immediately.",
    "invalid_payload": {
        "note": "This request is missing the 'features' key entirely."
    },
    "_technical_note": "This payload validates that the API enforces its input contract and does not allow incomplete requests to reach the model layer.",
    "_related_test_cases": {
        "test_1": "This file - missing 'features' key",
        "test_2": "Features provided as a string instead of a list",
        "test_3": "Non-numeric value inside the feature list",
        "test_4": "Incorrect number of feature values"
    }
}
