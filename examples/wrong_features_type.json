{
    "_comment": "INVALID test payload for API input validation",
    "_purpose": "This file intentionally provides the 'features' field with an incorrect data type in order to test whether the API correctly enforces that 'features' must be a list of numeric values.",
    "_error_type": "Invalid container type for features",
    "_specific_error": {
        "description": "The 'features' field is provided as a string instead of a list.",
        "received_type": "string",
        "expected_type": "list[float]",
        "example_expected_format": "{ \"features\": [14.2, 20.3, ...] }"
    },
    "_usage": "curl -X POST http://127.0.0.1:5000/predict -H 'Content-Type: application/json' -d @wrong_features_type.json",
    "_expected_response": {
        "http_code": 400,
        "error_type": "Bad Request / Validation Error",
        "message": "'features' must be a list of numeric values."
    },
    "_model_impact": "The model requires a numeric feature vector. If 'features' is not a list, the API cannot reshape or preprocess the input correctly, so inference must be rejected.",
    "features": "14.2, 20.3, 92.4, ... (this should be a list, not a string)",
    "_technical_note": "This payload ensures strict schema validation before any conversion into NumPy arrays or model.predict() calls.",
    "_related_test_cases": {
        "test_1": "Missing 'features' key",
        "test_2": "This file - features provided as a string instead of a list",
        "test_3": "Non-numeric value inside the feature list",
        "test_4": "Incorrect number of feature values"
    }
}
